specVersion: "v1"
serviceId: "analytics"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "analytics-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
